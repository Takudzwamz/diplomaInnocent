@page
@model StorefrontRazor.Pages.RecommendationTrackingModel
@{
    // This page exists only for its POST handlers (AJAX endpoints).
    // It should never be rendered directly.
    Response.Redirect("/");
}
